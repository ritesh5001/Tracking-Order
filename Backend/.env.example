# Backend environment variables
NODE_ENV=production
PORT=5001
MONGO_URI=replace-with-your-connection-string
JWT_SECRET=replace-with-a-long-random-secret
# Comma separated list of allowed origins (production)
FRONTEND_ORIGIN=https://your-frontend-domain
PORT=3000
MONGO_URI=mongodb+srv://rgiri5001_db_user:Ritesh5001@cluster0.xlwrnk0.mongodb.net/Shipping-Data
JWT_SECRET=replace_with_a_random_secret
# Optional: restrict CORS to these origins (comma-separated). Example:
# FRONTEND_ORIGIN=https://your-frontend.example.com,https://staging-frontend.example.com
FRONTEND_ORIGIN=